<?php

namespace FctBundle\Repository;
use FctBundle\Entity\Profesor;
use Doctrine\ORM\Tools\Pagination\Paginator;

/**
 * ProfesorRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class ProfesorRepository extends \Doctrine\ORM\EntityRepository
{
    public function getPaginateEntries($currentPage=1,$pageSize=5){
        $em = $this->getEntityManager();
        
        $dql = "SELECT p FROM FctBundle\Entity\Profesor p ORDER BY p.id ASC";
        
        $query = $em->createQuery($dql)
                ->setFirstResult($pageSize*($currentPage - 1))
                ->setMaxResults($pageSize);
        
        $paginator = new Paginator($query, $fetchJoinCollection = true);
    
        return $paginator;
    }
}
